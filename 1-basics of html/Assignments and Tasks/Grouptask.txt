Write a function which:
1. print the sum of the array's elements
  function simpleArraySum(arr) {
	  
  }
2. returns largest of 3 numbers
	function largestOfThree(a,b,c){ 
	}
3. return the nth fibonacci number
  f(0) = 0
  f(1) = 1
  f(10) = 55
  function fibonacci(n){
	  
  }
4. checks if a given input is a palindrome
  function palindrome(input){
  
  }
Test cases: 
a.) palindrome(“race car”) should return true
b.) palindrome(“not a palindrome”) should return false
c.) palindrome(“A man, a plan, a canal. Panama”) should return true

Note: Do not use replace(), split(), join(), reverse(). But you can use Array.push(), string.lowercase().